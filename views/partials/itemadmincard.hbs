<div class="card text-white" style="width: 22rem;">
    <a href="/menu/{{this._id}}">
        <div class="img-wrapper">
            <img src="{{this.image_url}}" class="card-img-top" alt="...">
        </div>
    </a>
    <div class="card-body d-flex flex-column justify-content-between text-white">
        <h5 class="card-title">{{this.title}}</h5>
        <p class="card-text">
            Price: € {{this.price}}<br />
            <span class="description-excerpt text-white">{{this.description}}</span>
        </p>
        <div class="container-fluid d-flex justify-content-around">
            <a href="/menu/{{this._id}}" class="btn btn-outline-primary">More details</a>
            <a href="/menu/{{this._id}}/edit" class="btn btn-outline-primary">Edit</a>
            <form action="/menu/{{this._id}}/delete" method="POST" class="d-inline">
                <button class="btn btn-outline-danger">Delete</button>
            </form>
        </div>
    </div>
</div>